# Python
__pycache__/
*.py[cod]
*.pyo
venv/
.env
.env.*

# Node.js (alle node_modules + Next.js Build — Haupt-Ursache für "too many active changes")
node_modules/
**/node_modules/
shared/renderer/package-lock.json
.next/
**/.next/
*.tsbuildinfo
**/*.tsbuildinfo

# Prisma / SQLite DB (enthält Personendaten — DSGVO, NIE committen)
*.db
*.db-journal
**/*.db
**/prisma/**/*.db

# Outputs (generated files — not committed)
outputs/

# macOS
.DS_Store

# Obsidian (local UI state — not project knowledge)
.obsidian/
**/.obsidian/
knowledge/2026-*.md
knowledge/*.canvas

# Python virtual environments (alle Varianten)
.venv*/
venv*/
_dfss_tmp/
